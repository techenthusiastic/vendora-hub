# Environment configuration
# NODE_ENV values:
# development - local dev
# production  - live app
# test        - test environment
# staging     - pre-production
NODE_ENV=development

# Server host - hostname or IP address where the app will bind
SERVER_HOST=localhost

# Server port
SERVER_PORT=3000

# Database connection settings
DB_HOST=localhost          # Database host (hostname or IP)
DB_PORT=5432               # Database port (default PostgreSQL port)
DB_USER=postgres           # Database username
DB_PASSWORD=your_password  # Database password (minimum 8 characters)
DB_NAME=nestjs_app         # Database name

# JWT authentication settings
JWT_SECRET=your_jwt_secret_key_with_min_32_characters
JWT_EXPIRATION=1h          # JWT token expiry (e.g. 30m, 1h, 15d)
JWT_ISSUER=your_issuer     # JWT token issuer
JWT_AUDIENCE=your_audience # JWT token audience

# Logger level - options: error, warn, info, debug, verbose
LOG_LEVEL=debug

# CORS origins - comma-separated list or single URL allowed for cross-origin requests
CORS_ORIGIN=http://localhost:4200

# Email SMTP server settings
EMAIL_HOST=smtp.example.com    # SMTP host for sending emails
EMAIL_PORT=587                 # SMTP port, usually 587 or 465